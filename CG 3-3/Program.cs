using System;

namespace CG_3_3
{
    class Program
    {
        static void Main(string[] args)
        {
            //statements are an action that a program takes,ends in semi colon                
            int x = 3;
            int y = 4;
            int z = x + y //is the expression
            //operators are the symbols that specify the operation of the action + - * /
            //operands are the players ie the integers x y and z are operands
            Console.WriteLine("find the value of z");
            Console.ReadLine();

        }

        
        }
    }
}
